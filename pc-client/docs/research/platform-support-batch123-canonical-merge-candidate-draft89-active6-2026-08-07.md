# Product platformSupport Batch 1–3 canonical merge candidate

Status: **candidate only** and not publishable. This is the sole recommended
consumption input for Batches 1–3; each research file remains the immutable
first-party evidence source.

The proposal attaches exact `platformSupport` evidence to 90 disjoint products
only. It has 271 claims: 209 supported, 58 unknown, 4 unsupported, and zero
blocked. The other 525 products, all 146 resources, 513 targets, and four
resource stores are unchanged.

Every claim must be read from its exact source record and pass the shared
candidate adapter before any future authorized save. A supported product claim
is display/filter evidence only: it grants no artifact, profile, lifecycle,
receipt, action, managed install, or Agent permission. Unknown is not upgraded
from host, package, browser, or network observations. In particular, OpenCode
retains its recorded Windows WSL supported, Windows native unknown, macOS
native unknown, and Linux native supported claims.

The active schema remains unbound. Rollback is discarding this candidate; a
future separately authorized consumption must remove only these 90 fields
after rechecking all recorded hashes and the draft89/v1 active72/v2 active6
baseline.
