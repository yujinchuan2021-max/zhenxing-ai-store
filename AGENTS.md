# AI Hub workspace instructions

- CTO 调度 AIhub 项目员工前，必须完整读取 `pc-client/docs/cto-operating-manual.md`。
- 只使用 AIhub 项目里的正式员工任务；当前最多同时运行两名员工，不创建临时协作子代理。
- 按任务选择模型：机械整理用 Luna，普通实现用 Terra，只有架构、安全或疑难诊断用 Sol。
- 员工遵守 `pc-client/docs/team-ownership-and-coordination.md` 的职责边界；跨模块结果同时回传依赖员工和 CTO。
- 本地开发优先；未经用户明确下令，不自动封包、发布、上传云端或永久删除。
